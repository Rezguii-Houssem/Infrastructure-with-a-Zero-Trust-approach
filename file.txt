#🏁 Final Project Checkpoint
#You have built a high-level, production-ready Terraform project. Let's look at what you’ve accomplished:

#Modular Architecture: Network and Compute are separated.

#Security: Private subnets with SSM access and zero public IPs.

#Remote State: S3 for storage, DynamoDB for locking.

#Scalability: Workspaces for Dev/Prod and Maps for configuration.

#Safety: Workspace guards to prevent default deployments.

#Intelligence: Data sources for dynamic AMI discovery.


"""Category	Command	Purpose
Setup	terraform init	Downloads providers and initializes the backend.
Health Check	terraform validate	Checks your code for syntax errors and logic bugs.
The "Dry Run"	terraform plan	Shows exactly what AWS will build before it happens.
Execution	terraform apply	Creates your infrastructure. (Always type yes).
Cleanup	terraform destroy	Deletes everything to save money.
Auth (SSO)	aws configure sso	Sets up secure, temporary access to AWS.
Auth (Login)	aws sso login --profile <name>	Refreshes your "keys" for the day.
Workspaces	terraform workspace new <env>	Creates a new environment (e.g., dev or prod).
Workspaces	terraform workspace list	Shows all active environments.
Workspaces	terraform workspace select <env>	Switches your terminal to a specific environment.
State	terraform init -migrate-state	Moves your local .tfstate file to the S3 bucket """

aws dynamodb create-table --table-name the-alpha-project-state-locks --billing-mode PAY_PER_REQUEST --attribute-definitions AttributeName=LockID,AttributeType=S --key-schema AttributeName=LockID,KeyType=HASH --region eu-west-3


aws s3api create-bucket --bucket the-alpha-project-terraform-state --region eu-west-1 --create-bucket-configuration LocationConstraint=eu-west-1

 aws s3api put-bucket-versioning --bucket the-alpha-project-terraform-state --versioning-configuration Status=Enabled


aws s3api put-public-access-block \
  --bucket the-alpha-project-terraform-state \
  --public-access-block-configuration \
  BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true




